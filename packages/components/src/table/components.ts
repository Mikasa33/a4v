import type { Component } from 'vue'
import { AViewTag } from '../view-tag'
import { AViewTime } from '../view-time'

export const components: Record<string, Component> = {
  AViewTag,
  AViewTime,
}
